# Changes Made in IETHRegistrarController

1. Remove `secret` parameter from `register` function
2. Remove every function from `IETHRegistrarController` interface except `available`, `register`, and `renew`.